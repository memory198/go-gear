# go-gear 开发约定

本文件由 pi 在启动时自动加载，用于跨会话维持本仓库的开发约定。

## 工作流程

- **改动前先给方案**：任何代码/文档改动先向用户说明方案与影响面，用户确认后再动手
- **只提交不推送**：允许执行 `git commit`，禁止任何 `git push` 操作（推送由用户自己完成）

## 代码规范

- 改动后必须运行：`go build ./...`、`go vet ./...`、`go test ./...`
- 遵循现有 API 风格与注释语言（中文注释、导出的类型/函数带文档注释）

## 行尾约定（重要）

- 本仓库文件为 **CRLF 行尾**（全局 `core.autocrlf=true` + `core.safecrlf=true`）
- 工具写入的文件常为 LF，提交前需转换：`sed -i 's/\r*$/\r/' <文件>`
- `gofmt -w` 会把文件转为 LF，使用后需重新转回 CRLF
- 勿删除或修改 `.gitignore` 中与行尾相关的配置

## 文档约定

- 文档使用**中文命名**（如 `使用指南.md`、`架构设计.md`），位于 `docs/`
- `docs/dev/` 记录开发方案：概览见 `方案概览.md`（只维护状态索引），每个方案一篇文档，命名 `YYYY-MM-DD-方案概括.md`，进度记录在方案文档内
- 文档之间的相对链接需完整可校验
